import React from 'react'
import styles from './Nav.module.css'

const Nav = (props) => {
    return (
        <div className={styles.nav}>
            <div>

            </div>
        </div>
    )
}

export default Nav